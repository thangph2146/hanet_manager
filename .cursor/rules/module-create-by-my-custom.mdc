---
description: 
globs: 
alwaysApply: false
---
---
description: 
globs: 
alwaysApply: true
---
# bạn hãy tham khảo dữ liệu TABLE nam_hoc (
    nam_hoc_id INT AUTO_INCREMENT PRIMARY KEY,
    ten_nam_hoc VARCHAR(50) NOT NULL,
    ngay_bat_dau DATE NULL,
    ngay_ket_thuc DATE NULL,
    status TINYINT(1) DEFAULT 1,
    bin TINYINT(1) DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL ON UPDATE CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP NULL,
# tham khảo module nguoidung app\Modules\loainguoidung, và đổi tên module vừa copy là namhoc
# sửa tên model dựa theo dữ liệu namhoc theo chuẩn tài liệu https://codeigniter4.github.io/userguide/models/model.html, và kiểm tra @BaseModel.php đã có chức nào có sẵn chưa, nếu chưa có thì tạo thêm, để model vừa tạo có thể xử dụng
# sửa entity dựa theo dữ liệu nam_hoc theo chuẩn tài liệu https://codeigniter4.github.io/userguide/models/entities.html và kiểm tra @BaseEntity.php có cần phải kế thừa không không, nếu không cần, thì tạo mới.
# các chức năng, file giữ nguyên, chỉ đổi theo dữ liệu nam_hoc, chỉ như vậy là đủ, bạn không cần tạo file mới, chỉ giữ nguyên và sửa đổi, copy và đổi theo dữ liệu table mình đã cung cấp.
